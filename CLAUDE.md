# CLAUDE.md

このファイルは Claude Code（および人間の開発者）がこのリポジトリで作業するための指示書です。

## プロダクト概要

**よりそい / Yorisoi** — 身近な人が陰謀論や極端な情報にハマってしまい、どう接すればよいか
分からず疲れている人を支援するモバイルファーストの Web アプリ。

> 「正しい情報を届ける前に、まず会話を終わらせない。」

支援対象は **相談者本人（悩んでいる側）** であって、ハマっている当事者を論破・矯正する
ツールではありません。

## 絶対に守るルール（UI 文言・AI 回答・体験談すべて）

このプロダクトはセンシティブな領域を扱います。以下は機能より優先されます。

1. **相手の心理状態を断定・診断しない** — 「〜という状態です」ではなく「〜かもしれません」
2. **相手を「陰謀論者」とラベリングしない** — 人ではなく状況を描写する
3. **相手を必ず変えられると約束しない** — 効果を保証する表現を書かない
4. **危険な状況では利用者自身の安全を最優先** — 「無理に話さなくていい」を必ず逃げ道として残す
5. **実在の個人・団体・特定の思想を攻撃しない**
6. トーンは暗い・攻撃的・監視的にしない。安心感・やさしさ・信頼感を優先する
7. 警告表示は赤黒ではなく **やさしい警告色** を使う（`AdviceCard` の tone を参照）

文言を追加・変更する PR では、この 7 項目に反していないかを必ず自己チェックしてください。

## 技術スタック

- React 19 / Vite 8 / React Router v7
- Tailwind CSS v4（`@tailwindcss/vite`。**設定ファイルはなく `src/index.css` の `@theme` がデザイントークン**）
- lucide-react（アイコン）
- oxlint（ESLint ではない）

## コマンド

```bash
npm install
npm run dev      # http://localhost:5173/
npm run build
npm run preview
npm run lint     # oxlint
```

## ディレクトリの約束

```
src/
  config.js       # サービス名・共通コピー。名称や文言の変更はまずここを見る
  App.jsx         # ルーティング＋モバイルシェル（最大430px）
  index.css       # Tailwind＋デザイントークン（@theme）
  components/     # 再利用パーツ
  pages/          # 画面 = ルート1つにつき1ファイル
  data/           # モックデータ＋取得関数（将来 API に差し替える境界）
```

## 実装時の指針

- **モバイルファースト**。最大幅 430px 前提。PC では中央寄せのスマホ幅で表示される
- 色・余白・角丸は直接の値ではなく `src/index.css` の `@theme` トークンを使う
- 新しい画面を足したら `App.jsx` のルートと、必要なら `BottomNav.jsx` を更新する
- API 差し替えの境界は `src/data/*`。**UI から直接 fetch を書かない**
- AI 回答の構造（7セクション）は `mockAdvice.js` の形を崩さない。LLM 接続時も同じ JSON 形にする
- 秘密鍵・API キーを**絶対にコミットしない**。`.env.local` を使い `.env.example` に名前だけ書く
- 依存パッケージの追加は最小限に。追加する場合は PR の説明に理由を書く

## やらないこと

- 大規模リファクタを頼まれていないのに実行しない（ハッカソン期間中は動くことが最優先）
- `package-lock.json` を手で書き換えない
- モックデータを消さない（デモで使う）

## 使える Skill

`.claude/skills/` に置いてあり、チーム全員で共有される。

- **`start-issue`** — Issue に着手するときの入り口。`gh issue develop` で Issue から
  ブランチを生やし、assign とボードの状態を合わせる。**作業はここから始める**
- **`pr`** — コミット・push・PR 作成。main は保護されていて直 push できないので、
  変更を出すときは基本これを通す。PR タイトルの規約や動作確認の埋め方も面倒を見る
- **`tone-check`** — 利用者に見える日本語が上の「絶対に守るルール」7項目に反していないか検査する。
  文言を足した・変えたときと、PR の思想チェック欄を埋める前に実行する

## Git / PR の約束

- ブランチ: `<type>/<Issue番号>-<英語の要約>`（例 `feat/12-story-filter`）。`gh issue develop` で Issue から生やす
- タスクの現在地は [開発ボード](https://github.com/orgs/yuai-project/projects/1)。`In Review` への移動だけ手動
- main への直 push はしない。必ず PR を経由する
- PR タイトルは `feat: 体験談の絞り込みを実装` のように Conventional Commits 形式
- 詳細は [CONTRIBUTING.md](CONTRIBUTING.md) を参照
